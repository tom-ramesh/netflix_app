import 'package:flutter/cupertino.dart';

kWidth(double size) => SizedBox(
      width: size,
    );

kHeight(double size) => SizedBox(
      height: size,
    );

//BorderRadius
BorderRadius kRadius(double radius) => BorderRadius.circular(radius);

//TextStyle
TextStyle kTextStyle14 = const TextStyle(
  fontSize: 14,
  fontWeight: FontWeight.bold,
);
